# 教育知识库项目方案设计

> 本文档用于指导教育知识库项目的技术实现。文档基于掌柜智库项目的技术选型和工程思路进行教育行业适配，重点描述系统架构、模块边界、数据流、目录规划、数据模型、接口设计、检索问答流程和分期开发落点。

---

## 1. 设计目标

本方案需要支撑《01_教育知识库需求分析.md》中定义的全部建设范围，并为后续开发提前铺好工程基础。

### 1.1 一期设计目标

- 复用掌柜智库的 RAG 技术路线，快速完成教育知识库基础版。
- 支持课程资料、项目文档、讲义和题库导入。
- 支持课程检索、文档检索、题库检索和知识问答。
- 支持流式输出、会话历史和引用来源返回。
- 保持模块分层清晰，方便后续扩展后台、权限、视频、多模态等能力。

### 1.2 长期设计目标

- 支持多内容类型：文档、题目、课程、项目、视频、图片、课件截图。
- 支持多检索策略：混合向量、关键词、HyDE、元数据过滤、视频片段检索、多模态检索。
- 支持多租户与权限过滤。
- 支持文档增量更新和版本管理。
- 支持学习路径推荐、能力测评和学习画像。

---

## 2. 总体架构

系统采用分层架构：

| 层级 | 职责 | 技术选型 |
|------|------|----------|
| API 层 | 暴露 HTTP 接口，处理请求校验和响应封装 | FastAPI + Uvicorn |
| Service 层 | 组织业务服务，连接 API 和流程编排 | Python Service 模块 |
| Processor 层 | 编排导入、查询、视频检索、多模态检索流程 | LangGraph |
| Schema 层 | 定义请求、响应、任务、课程、题目等数据模型 | Pydantic |
| Utils 层 | 封装向量库、模型、对象存储、数据库和 SSE 工具 | Python 工具模块 |
| Data 层 | 存储向量、元数据、文件和历史记录 | Milvus / MongoDB / MinIO |
| Front 层 | 提供聊天、导入和后续管理页面 | HTML5 + JS，后续可升级 |

### 2.1 技术栈选型

| 类别 | 技术选型 | 用途 |
|------|----------|------|
| 后端框架 | FastAPI + Uvicorn | 异步 HTTP 服务 |
| 工作流编排 | LangGraph | 导入流程、查询流程、扩展流程编排 |
| LLM | 阿里云 DashScope Qwen | 问题改写、答案生成、摘要生成 |
| 视觉语言模型 | qwen3-vl-flash | 图片、课件截图、视频关键帧理解 |
| 向量模型 | BGE-M3 / text-embedding-v4 | 稠密和稀疏向量生成 |
| 重排序模型 | BGE-Reranker-Large | 候选片段重排序 |
| 向量数据库 | Milvus | 文档、题目、视频片段和多模态向量检索 |
| 文档数据库 | MongoDB | 会话历史、任务状态、课程元数据 |
| 对象存储 | MinIO | 原始文件、课件图片、视频关键帧 |
| PDF 解析 | MinerU | PDF 转 Markdown |
| DOCX 解析 | python-docx / Mammoth / LibreOffice 可选链路 | Word 课程资料、讲义、题库说明解析 |
| 流式协议 | SSE | 实时返回问答内容 |

---

## 3. 工程目录规划

后续开发建议使用以下目录结构。命名保持教育领域语义，不再沿用掌柜智库中的商品识别等垂直命名。

```text
edu_rag/
├── api/
│   ├── import_router.py             # 内容导入接口
│   ├── query_router.py              # 问答查询接口
│   ├── course_router.py             # 课程查询接口
│   ├── question_router.py           # 题库查询接口
│   ├── video_router.py              # 视频检索接口，五期启用
│   └── admin_router.py              # 后台管理接口，二期启用
│
├── core/
│   ├── config.py                    # 环境配置
│   ├── deps.py                      # 依赖注入和单例管理
│   ├── enums.py                     # 内容类型、任务状态、角色枚举
│   └── paths.py                     # 路径配置
│
├── schema/
│   ├── import_schema.py             # 上传与导入任务模型
│   ├── query_schema.py              # 查询请求与响应模型
│   ├── course_schema.py             # 课程模型
│   ├── question_schema.py           # 题目模型
│   ├── chunk_schema.py              # 知识片段模型
│   ├── video_schema.py              # 视频片段模型
│   └── admin_schema.py              # 后台管理模型
│
├── services/
│   ├── import_service.py            # 导入业务服务
│   ├── query_service.py             # 查询业务服务
│   ├── course_service.py            # 课程业务服务
│   ├── question_service.py          # 题库业务服务
│   ├── history_service.py           # 会话历史服务
│   ├── task_service.py              # 任务状态服务
│   └── admin_service.py             # 后台管理服务
│
├── processor/
│   ├── import_process/
│   │   ├── main_graph.py            # 导入流程图
│   │   ├── state.py                 # 导入状态
│   │   └── nodes/
│   │       ├── entry.py             # 入口节点
│   │       ├── file_parse.py        # 文件解析
│   │       ├── pdf_to_md.py         # PDF 转 Markdown
│   │       ├── docx_to_md.py        # DOCX 转 Markdown/结构化文本
│   │       ├── metadata_extract.py  # 课程、章节、知识点元数据抽取
│   │       ├── document_split.py    # 文档切分
│   │       ├── question_parse.py    # 题库解析
│   │       ├── video_transcript.py  # 视频字幕解析，五期启用
│   │       ├── embedding.py         # 向量化
│   │       └── import_store.py      # 入库
│   │
│   ├── query_process/
│   │   ├── main_graph.py            # 查询流程图
│   │   ├── state.py                 # 查询状态
│   │   ├── prompt.py                # 提示词模板
│   │   └── nodes/
│   │       ├── query_rewrite.py     # 问题改写
│   │       ├── intent_recognize.py  # 查询意图识别
│   │       ├── metadata_filter.py   # 元数据过滤条件生成
│   │       ├── vector_search.py     # 混合向量检索
│   │       ├── hyde_search.py       # HyDE 检索
│   │       ├── question_search.py   # 题库专项检索
│   │       ├── video_search.py      # 视频片段检索，五期启用
│   │       ├── rrf.py               # 多路召回融合
│   │       ├── rerank.py            # 重排序
│   │       └── answer_output.py     # 答案生成
│   │
│   └── multimodal_process/          # 六期启用
│       ├── main_graph.py
│       └── nodes/
│           ├── image_ocr.py
│           ├── image_caption.py
│           ├── keyframe_extract.py
│           └── multimodal_search.py
│
├── utils/
│   ├── milvus_utils.py              # Milvus 操作
│   ├── mongo_utils.py               # MongoDB 操作
│   ├── minio_utils.py               # MinIO 操作
│   ├── embedding_utils.py           # 向量模型封装
│   ├── reranker_utils.py            # 重排序模型封装
│   ├── llm_utils.py                 # LLM 客户端封装
│   ├── sse_utils.py                 # SSE 推送
│   ├── hash_utils.py                # 文件和片段哈希，二期增量更新使用
│   └── time_utils.py                # 时间处理
│
├── front/
│   ├── chat.html                    # 聊天页面
│   ├── import.html                  # 导入页面
│   └── admin.html                   # 管理页面，二期启用
│
├── docs/
│   ├── 01_教育知识库需求分析.md
│   └── 02_教育知识库方案设计.md
│
├── tests/
├── temp_data/
├── .env.example
└── requirements.txt
```

---

## 4. 数据存储设计

### 4.1 Milvus 集合规划

| 集合名 | 用途 | 一期是否启用 |
|--------|------|--------------|
| `edu_kb_chunks` | 文档片段、课程介绍、项目资料向量 | 是 |
| `edu_questions` | 题干、选项、解析向量 | 是 |
| `edu_course_names` | 课程名、项目名、章节名等实体辅助检索 | 建议 |
| `edu_video_segments` | 视频字幕、讲稿片段向量 | 五期 |
| `edu_multimodal_assets` | 图片、截图、关键帧向量 | 六期 |

### 4.2 MongoDB 集合规划

| 集合名 | 用途 |
|--------|------|
| `courses` | 课程基础信息 |
| `chapters` | 章节信息 |
| `projects` | 项目信息 |
| `questions` | 题目结构化信息 |
| `documents` | 文件记录、版本、来源路径 |
| `chunks` | 知识片段元数据 |
| `import_tasks` | 导入任务状态 |
| `chat_history` | 会话历史 |
| `video_segments` | 视频片段元数据，五期 |
| `tenants` | 租户信息，三期 |
| `users` | 用户信息，三期 |
| `audit_logs` | 管理端审计日志，三期 |

### 4.3 MinIO 存储规划

| 路径前缀 | 用途 |
|----------|------|
| `raw/{tenant_id}/{course_id}/` | 原始上传文件 |
| `parsed/{tenant_id}/{course_id}/` | PDF 转换后的 Markdown、图片 |
| `courseware/{tenant_id}/{course_id}/` | 课件页面图片 |
| `video/{tenant_id}/{course_id}/` | 视频字幕、讲稿、关键帧 |
| `exports/{tenant_id}/` | 后续导出文件 |

---

## 5. 核心数据模型

### 5.1 KnowledgeChunk

```json
{
  "chunk_id": "chunk_001",
  "content": "知识片段正文",
  "content_type": "document_chunk",
  "course_id": "course_001",
  "course_name": "Python 入门",
  "chapter_id": "chapter_001",
  "chapter_name": "函数基础",
  "project_id": "project_001",
  "project_name": "学生成绩管理系统",
  "knowledge_points": ["函数", "参数", "返回值"],
  "source_file_name": "函数基础.md",
  "source_path": "raw/default/course_001/函数基础.md",
  "page_no": null,
  "version": "v1",
  "tenant_id": "default",
  "is_active": true,
  "content_hash": "sha256...",
  "created_at": "2026-05-19T20:00:00+08:00",
  "updated_at": "2026-05-19T20:00:00+08:00"
}
```

### 5.2 Question

```json
{
  "question_id": "q_001",
  "question_code": "PY-FUNC-001",
  "question_bank_name": "Python 基础题库",
  "question_type": "single_choice",
  "stem": "以下哪个关键字用于定义函数？",
  "options": ["func", "def", "lambda", "return"],
  "answer": "B",
  "analysis": "Python 使用 def 定义普通函数。",
  "difficulty": "easy",
  "course_id": "course_001",
  "course_name": "Python 入门",
  "chapter_id": "chapter_001",
  "chapter_name": "函数基础",
  "knowledge_points": ["函数定义"],
  "tenant_id": "default"
}
```

### 5.3 VideoSegment

```json
{
  "video_id": "video_001",
  "video_title": "RAG 检索流程讲解",
  "course_id": "course_002",
  "course_name": "RAG 项目实战",
  "chapter_id": "chapter_003",
  "chapter_name": "检索增强生成",
  "segment_id": "seg_001",
  "start_time": "00:05:12",
  "end_time": "00:07:30",
  "transcript": "这一段我们讲 Milvus 的混合检索流程...",
  "summary": "讲解 Milvus 稠密向量与稀疏向量混合召回。",
  "source_url": "https://example.com/video_001",
  "keyframe_path": "video/default/course_002/video_001/keyframes/seg_001.jpg",
  "tenant_id": "default"
}
```

---

## 6. 导入流程设计

### 6.1 导入流程节点

导入流程由 LangGraph 编排。

```text
entry
  -> save_raw_file
  -> file_parse
  -> metadata_extract
  -> document_split / question_parse / video_transcript
  -> embedding
  -> import_store
  -> task_done
```

### 6.2 文件类型处理策略

| 文件类型 | 处理方式 |
|----------|----------|
| PDF | MinerU 转 Markdown，再切分 |
| Markdown | 直接读取，按标题和长度切分 |
| DOCX/Word | 一期正式支持；优先解析标题、段落、列表和表格，转换为 Markdown 或结构化文本 |
| Excel/CSV 题库 | 结构化解析为 Question |
| JSON 题库 | 直接映射为 Question |
| SRT/VTT 字幕 | 按时间轴切分为 VideoSegment |
| PPT/PDF 课件 | 先提取文本和页码，后续提取截图 |

### 6.2.1 DOCX 解析策略

Word 文档在教育资料中通常承担课程大纲、讲义、项目说明、题库讲解和补充资料的角色。一期应将 `.docx` 纳入正式导入链路。

解析策略：

- 使用 `python-docx` 读取段落、标题样式、编号列表和表格文本。
- 对标题样式如 Heading 1、Heading 2、Heading 3 建立章节层级。
- 对普通段落按原始顺序拼接，保留段落边界。
- 对表格按行列转换为 Markdown 表格或“字段：值”文本，保证可检索。
- 对 Word 中的图片、复杂公式、文本框等无法稳定解析的内容，先记录占位和告警，后续多模态阶段再处理。
- 可选使用 Mammoth 将 `.docx` 转为 HTML/Markdown，用于保留更接近原文的语义结构。
- 可选使用 LibreOffice 做格式转换兜底，但不应作为唯一解析方案。

DOCX 解析后的统一输出建议为：

```json
{
  "source_file_name": "尚硅谷大模型技术之Python2.0.docx",
  "document_format": "docx",
  "title_tree": ["Python 基础", "函数", "参数与返回值"],
  "blocks": [
    {
      "block_type": "heading",
      "level": 2,
      "text": "函数基础"
    },
    {
      "block_type": "paragraph",
      "text": "函数是组织代码的重要方式..."
    },
    {
      "block_type": "table",
      "text": "| 题型 | 数量 | 知识点 |..."
    }
  ],
  "warnings": []
}
```

### 6.3 切片策略

文档切片需要兼顾语义完整性和检索效果。

- 优先按 Markdown 标题、Word 标题样式、章节、小节切分。
- 单个片段建议控制在 300 到 800 中文字之间。
- 保留标题路径，如课程名、章节名、小节名。
- 片段之间可保留适当 overlap。
- 题目不按普通文档切分，应保持题干、选项、答案、解析为一个完整对象。
- 视频字幕按时间窗口切片，建议 30 到 90 秒为一个片段。
- DOCX 中的表格如果表达课程大纲、题目列表或项目步骤，应优先按表格行或逻辑分组切片，避免整张大表混入一个片段。

### 6.4 元数据补全

导入阶段需要尽量补全：

- 课程名称。
- 章节名称。
- 项目名称。
- 知识点。
- 题库名称。
- 来源文件名。
- 来源路径。

元数据来源优先级：

1. 用户上传时填写的表单。
2. 文件路径和文件名。
3. 文档标题结构。
4. LLM 辅助抽取。
5. 默认值。

---

## 7. 查询流程设计

### 7.1 查询流程节点

```text
query_entry
  -> load_history
  -> query_rewrite
  -> intent_recognize
  -> metadata_filter
  -> parallel_retrieve
      -> vector_search
      -> hyde_search
      -> question_search
      -> video_search
  -> rrf_fusion
  -> rerank
  -> cutoff
  -> answer_output
  -> save_history
```

### 7.2 意图类型

| 意图 | 说明 | 主要召回源 |
|------|------|------------|
| `course_intro` | 查询课程介绍或推荐课程 | courses / edu_kb_chunks |
| `course_detail` | 查询课程详情和章节结构 | courses / chapters |
| `document_search` | 查询讲义、项目文档、知识点 | edu_kb_chunks |
| `question_search` | 查询题目、答案、解析 | edu_questions / questions |
| `project_qa` | 查询项目步骤、配置、报错 | edu_kb_chunks |
| `video_search` | 查询视频片段 | edu_video_segments |
| `general_qa` | 综合问答 | 多路召回 |

### 7.3 检索策略

一期至少实现：

- 原始问题混合向量检索。
- 改写问题混合向量检索。
- HyDE 检索。
- 题库专项检索。
- 元数据过滤。
- RRF 融合。
- Reranker 重排序。

后续扩展：

- 视频字幕片段检索。
- 图片 OCR 文本检索。
- 视觉向量检索。
- 权限过滤召回。

### 7.4 答案生成策略

答案生成提示词需要约束：

- 必须优先基于召回内容回答。
- 不确定时明确说明无法从当前知识库确认。
- 题目解析需要给出解题思路。
- 项目步骤需要给出步骤顺序。
- 课程推荐需要说明推荐理由。
- 输出引用来源，包含课程、章节、文件、题目或视频时间段。

---

## 8. API 设计

### 8.1 导入接口

#### POST `/upload`

用途：上传课程资料、讲义、项目文档、题库或字幕文件。

请求类型：`multipart/form-data`

字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| files | file[] | 是 | 上传文件 |
| course_name | string | 否 | 课程名称 |
| chapter_name | string | 否 | 章节名称 |
| project_name | string | 否 | 项目名称 |
| content_type | string | 否 | 内容类型 |
| tenant_id | string | 否 | 租户 ID，默认 default |

支持文件格式：

- `.pdf`
- `.md`
- `.docx`
- `.xlsx` / `.csv`
- `.json`
- `.srt` / `.vtt`

响应：

```json
{
  "message": "Files uploaded successfully",
  "task_ids": ["task_001"]
}
```

#### GET `/status/{task_id}`

响应：

```json
{
  "task_id": "task_001",
  "status": "processing",
  "progress": 45,
  "message": "正在向量入库...",
  "file_name": "Python函数基础.pdf",
  "created_at": "2026-05-19T20:00:00+08:00"
}
```

### 8.2 查询接口

#### POST `/query`

请求：

```json
{
  "query": "Python 函数怎么定义？",
  "session_id": "sess_001",
  "is_stream": true,
  "course_id": "course_001",
  "tenant_id": "default"
}
```

响应：

```json
{
  "message": "处理完成",
  "session_id": "sess_001",
  "answer": "Python 使用 def 关键字定义函数...",
  "references": [
    {
      "content_type": "document_chunk",
      "course_name": "Python 入门",
      "chapter_name": "函数基础",
      "source_file_name": "函数基础.md",
      "score": 0.91
    }
  ]
}
```

#### GET `/stream/{session_id}`

用于 SSE 流式输出。

事件示例：

```text
data: {"type": "delta", "data": {"delta": "Python"}}
data: {"type": "final", "data": {"status": "completed"}}
```

### 8.3 课程接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/courses` | 查询课程列表 |
| GET | `/courses/{course_id}` | 查询课程详情 |
| GET | `/courses/{course_id}/chapters` | 查询课程章节 |

### 8.4 题库接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/questions` | 查询题目列表 |
| GET | `/questions/{question_id}` | 查询题目详情 |

### 8.5 视频接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/video/search` | 检索教学视频片段 |
| GET | `/videos/{video_id}/segments/{segment_id}` | 查询视频片段详情 |

### 8.6 管理接口

二期启用。

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/admin/courses` | 新建课程 |
| PUT | `/admin/courses/{course_id}` | 更新课程 |
| POST | `/admin/kb/reindex` | 触发重建或增量更新 |
| GET | `/admin/tasks` | 查询导入和更新任务 |
| PUT | `/admin/chunks/{chunk_id}` | 编辑知识片段元数据 |

---

## 9. 前端页面规划

### 9.1 一期页面

| 页面 | 功能 |
|------|------|
| `import.html` | 上传课程资料、项目文档、讲义和题库，查看任务状态 |
| `chat.html` | 输入问题，查看流式答案、引用来源和历史记录 |

### 9.2 二期页面

| 页面 | 功能 |
|------|------|
| 课程管理 | 新增、编辑、查看课程 |
| 知识库管理 | 查看文档、片段、元数据和索引状态 |
| 题库管理 | 导入、编辑、查询题目 |
| 任务管理 | 查看导入和增量更新任务 |

### 9.3 后续页面

- 租户与权限管理。
- 学习路径与能力测评。
- 视频检索页面。
- 多模态检索页面。

---

## 10. 配置规划

`.env.example` 建议包含：

```ini
# LLM
OPENAI_API_KEY=
OPENAI_API_BASE=
LLM_DEFAULT_MODEL=qwen-flash
LLM_DEFAULT_TEMPERATURE=0.1
VL_MODEL=qwen3-vl-flash

# Embedding
BGE_M3_PATH=
BGE_DEVICE=cuda:0
BGE_FP16=True
BGE_RERANKER_LARGE=
BGE_RERANKER_DEVICE=cuda:0
EMBEDDING_MODEL=text-embedding-v4
EMBEDDING_DIM=1536

# Milvus
MILVUS_URL=http://localhost:19530
MILVUS_CHUNKS_COLLECTION=edu_kb_chunks
MILVUS_QUESTIONS_COLLECTION=edu_questions
MILVUS_COURSE_NAMES_COLLECTION=edu_course_names
MILVUS_VIDEO_SEGMENTS_COLLECTION=edu_video_segments
MILVUS_MULTIMODAL_COLLECTION=edu_multimodal_assets
MILVUS_METRIC_TYPE=COSINE
MILVUS_MIN_COSINE_SCORE=0.75

# MongoDB
MONGO_URL=mongodb://localhost:27017
MONGO_DB_NAME=edu_rag

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=edu-knowledge-base

# App
DEFAULT_TENANT_ID=default
IMPORT_SERVICE_PORT=8000
QUERY_SERVICE_PORT=8001

# Parser
DOCX_PARSE_MODE=python-docx
DOCX_ENABLE_TABLE_PARSE=true
DOCX_ENABLE_IMAGE_PLACEHOLDER=true
```

---

## 11. 开发顺序建议

### 11.1 基础工程搭建

1. 建立目录结构。
2. 编写 `core/config.py` 和 `.env.example`。
3. 封装 MongoDB、Milvus、MinIO、LLM、Embedding、Reranker 工具。
4. 定义 Schema。

### 11.2 导入链路

1. 实现上传接口。
2. 实现任务状态管理。
3. 实现 PDF/Markdown/DOCX 文件解析。
4. 实现元数据抽取和文档切分。
5. 实现向量化。
6. 实现 Milvus 和 MongoDB 入库。

### 11.3 查询链路

1. 实现查询接口和 SSE。
2. 实现历史记录。
3. 实现问题改写和意图识别。
4. 实现混合检索和题库检索。
5. 实现 RRF、Rerank、动态截断。
6. 实现答案生成和引用来源返回。

### 11.4 页面和联调

1. 实现上传页面。
2. 实现聊天页面。
3. 联调导入和查询。
4. 补充典型测试数据。
5. 验证课程、文档、题库、项目问答场景。

### 11.5 二期以后

1. 后台管理。
2. 增量更新。
3. 权限多租户。
4. 学习路径和能力测评。
5. 视频检索。
6. 多模态检索。

---

## 12. 测试策略

### 12.1 单元测试

- 文件解析。
- DOCX 段落、标题和表格解析。
- 文档切分。
- 题库解析。
- 元数据抽取。
- 向量入库参数构造。
- 查询意图识别。
- RRF 融合。
- 引用来源格式化。

### 12.2 集成测试

- 上传文件到任务完成。
- 文档检索到答案生成。
- 题目检索到答案解析。
- 多轮对话上下文续问。
- SSE 流式输出。

### 12.3 验收测试

围绕需求分析中的典型问题准备测试集：

- 课程介绍类问题。
- 课程详情类问题。
- 项目步骤类问题。
- 环境配置类问题。
- 题目解析类问题。
- 无答案类问题。

---

## 13. 风险与处理

| 风险 | 影响 | 处理方式 |
|------|------|----------|
| 元数据缺失 | 检索过滤和来源追溯不准确 | 上传表单、文件路径、LLM 抽取多路补全 |
| 题库格式不统一 | 解析失败或字段缺失 | 先定义标准模板，再兼容常见格式 |
| DOCX 排版复杂 | 标题、表格、图片、公式解析不完整 | 先稳定解析文本和表格，图片与复杂对象记录占位并在多模态阶段扩展 |
| 文档切片过碎或过长 | 检索效果差 | 按标题结构切分并控制片段长度 |
| 检索召回不稳定 | 答案不准确 | 混合检索、多路召回、Rerank 和引用约束 |
| 模型调用失败 | 查询不可用 | 增加异常处理和降级提示 |
| 后续权限改造成本高 | 数据隔离困难 | 一期即预留 tenant_id、user_id、role 字段 |
| 视频和多模态扩展复杂 | 后期重构风险 | 一期预留 content_type、asset_path、video_segment 字段 |

---

## 14. 方案结论

教育知识库项目应沿用掌柜智库的成熟 RAG 架构，以 FastAPI、LangGraph、Milvus、MongoDB、MinIO、BGE-M3、BGE-Reranker 和 Qwen 系列模型作为核心技术栈。一期优先完成内容导入、检索问答、题库查询、流式输出和来源追溯；同时在数据模型、目录结构、接口和流程中预留增量更新、权限多租户、视频检索和多模态检索的扩展点。这样后续进入开发时，可以按导入链路、查询链路、页面联调、扩展能力的顺序稳步推进。
